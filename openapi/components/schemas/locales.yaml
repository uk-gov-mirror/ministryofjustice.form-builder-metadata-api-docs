type: array
description: Metadata for other languages
items:
  type: object
  properties:
    locale:
      type: string
    version_id:
      type: string
  required:
    - locale
    - version_id
example: [
  {
    "local": "cy",
    "version_id": "1234"
  }
]
