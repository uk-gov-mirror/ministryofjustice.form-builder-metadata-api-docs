type: object
properties:
  service_name:
    description: User supplied name
    type: string
    minLength: 4
    example: Service name
  locale:
    description: Defaults to en. Or could be "cy"
    type: string
    example: en
  metadata:
    $ref: ./service.yaml
required:
  - service_name
  - metadata
