type: object
description: The response when some authorisation information is missing or invalid
properties:
  message:
    description: Response message
    type: array
    items:
      type: string
      minLength: 1
      example: Authorisation information is missing or invalid
example: {
  message: [
    'Authorisation information is missing or invalid'
  ]
}
