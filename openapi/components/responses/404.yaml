type: object
description: The error message returned for a Service that is not found
properties:
  message:
    description: Response message
    type: array
    items:
      type: string
      minLength: 1
      example: Requested Service not found
example: {
  message: [
    'Requested Service not found',
    'Requested Version not found'
  ]
}
